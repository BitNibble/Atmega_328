In C there are basically 4 or five styles of programming, following the syntax basically can solve any problem.

Here using one of them consistently, creating a fixed programming architecture, that is flexible, easy to maintain,
upgrade and add functionalities.

Just by sticking to the process.

That is why STM32 is seen as the most evolved programming structure comparing to other microcontroller networks,
but here take a different approach, which to me is more adequate, even though can easily be intertwined with STM32
architecture, using it as a piggy back, diferences can easily be converted from one to another, or use them both, 
which is not advised, because routine simplifies life, not worrying always which is which.

A microncontroller is what, but a cpu with modules, communication modules, timming modules, and ADC modules.

If one creates a structure that manipulates these very very well, then life will become so simple tha can make anything.
The problem is that most are complex, and to many types, normalization should be consistant. Just choose one.

That is why programming languages instead of helping just becomes barriers.

